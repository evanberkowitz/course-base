		\begin{tikzpicture}
			\begin{axis}[
					domain=0:5*pi,
					trig format=rad,
					axis x line=center,
					ylabel={$y$},
					xlabel={$\omega_0 t$}, 
					axis y line=middle,
					xmin=-0.5, xmax=16,
					ymin=-2, ymax=2,
					xtick={1*pi,2*pi,3*pi,4*pi,5*pi},
					xticklabels={$\pi$, $2\pi$, $3\pi$, $4\pi$, },
					yticklabels={}
				]
				\addplot[smooth, samples=100] {cos(x)}; 
			\end{axis}
		\end{tikzpicture}
